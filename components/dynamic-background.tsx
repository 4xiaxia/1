export function DynamicBackground() {
  return (
    // Adjusted gradient colors for a more elegant feel
    <div className="fixed inset-0 -z-20 bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50 animate-gradient-shift"></div>
  )
}
